import Foundation
import MCP

// MCPHTTPServer — Phase 1 PoC entry point. Filled out in the next
// commit. This stub exists so the SPM wire-up commit verifies the
// `import MCP` resolves without dragging in any in-flight scaffolding.

@MainActor
final class MCPHTTPServer {
    static let isMCPLibraryReachable = true
}
