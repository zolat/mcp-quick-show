{
  "name": "mcp-quick-show",
  "version": "0.1.0",
  "description": "MCP sidecar for QuickShow — render content into floating HUD panels and see the rendered result.",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "ping": "bun run src/cli/ping.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "bin": {
    "mcp-quick-show": "src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
